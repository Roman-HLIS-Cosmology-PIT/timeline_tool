# timeline_tool
Simple tool to help defining the analysis timeline for Roman

To run the notebook, one can create a conda environment using:
```bash
conda env create -f environment.yml
```
or just install the required libraries using the requirement file:
```bash
conda install -f requirements.txt -c conda-forge
```
or
```bash
pip install -r requirements.txt
```